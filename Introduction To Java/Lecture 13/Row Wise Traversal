public class Solution {
    public static int[] printRowWise(int [][]a) {
        // Write your code here.
        int n = a.length;
        int m = a[0].length;
        int[] result = new int[n * m];
        int index = 0;
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                result[index++] = a[i][j];
            }
        }
        return result;
    }
}
